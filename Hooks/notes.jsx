// import { useState } from "react"

// "A normal variable can store the value, but changing it does not tell React that the UI needs to be updated.
//  State is managed by React, and updating state triggers a re-render, which allows the updated value to be reflected in the UI."

// useState:- use to manage state (imagine ... there is a space to write a value in first case and  empty it a case of state...
    // but if there is a value its a second state, both are two diffrent state.
    //    for example number of likes in insta koh increase krna
// )

// useeffect:- yeh ek saath two diiferent types of function koh saath mein work karna allow krta. For example if a function is running
// and we want to call an api, we can work on both , the main function will running continuosly and the api will also do its assisted work.

//useref:- 

// usecontext:- for example the data is passing from App->section->section2 but i want to pass it from app to directly section2
// but in middle there is section which takes times and resources, so we takes help from useContext which can make the data global
// and it can be access by every part of the code.  

//useReducer:- use to manage the complex code, or complex global data, 

//usememo and usecallback :- it make sure no unneccessary element or component does not get render unecessarily